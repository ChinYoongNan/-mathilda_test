import { SecHasAccessToDirective } from './sec-has-access-to.directive';

describe('SecHasAccessToDirective', () => {
  it('should create an instance', () => {
    const directive = new SecHasAccessToDirective();
    expect(directive).toBeTruthy();
  });
});
